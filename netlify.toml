# Configuración de despliegue en Netlify.
# El runtime de Next.js de Netlify se detecta automáticamente e instala el
# plugin oficial; aquí solo fijamos el comando de build y la versión de Node.

[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"
